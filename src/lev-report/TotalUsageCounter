import React from 'react';
import styled from 'styled-components';

const StyledCounter = styled.div`
  position: absolute;
  right: 0;
  font-size: 50px
`;

const TotalUsageCounter = props => <StyledCounter>{props.count}</StyledCounter>;

export default TotalUsageCounter;
